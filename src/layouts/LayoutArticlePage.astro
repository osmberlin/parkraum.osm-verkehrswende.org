---
import LayoutArticle from './LayoutArticle.astro'
import clsx from 'clsx'

type Props = {
  title: string
  menuHighlight?: string | undefined
  noindex?: boolean | undefined
  maxWidthClass?: string | undefined
  language?: 'en' | 'de' | undefined
}
export type LayoutArticleProps = Props

const { title, menuHighlight, noindex, maxWidthClass, language } = Astro.props
---

<LayoutArticle title={title} menuHighlight={menuHighlight} noindex={noindex}>
  <article
    class={clsx(
      'prose prose-blue',
      maxWidthClass || 'max-w-prose',
      'px-4 py-6 lg:px-0 lg:pl-5',
      '[--tw-prose-bullets:_gray]',
      'prose-headings:scroll-mt-10 prose-h1:mb-3 prose-h1:mt-0 prose-h1:text-sky-700 prose-h2:mb-4 prose-h2:mt-16 prose-h3:mb-4 prose-h3:mt-8',
      'prose-code:rounded-sm prose-code:bg-neutral-100 prose-code:box-decoration-clone prose-code:px-1.5 prose-code:py-1 prose-code:text-neutral-800 prose-code:before:content-none prose-code:after:content-none',
    )}
    lang={language}
  >
    <slot />
  </article>
</LayoutArticle>
